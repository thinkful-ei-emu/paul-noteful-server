# Express Bookmark Server!

### Database setup

1. Create dev and test database, suggest: `bookmarks` and `bookmarks_test`
2. Add DB_URL and TEST_DB_URL in .env
3. 


psql -U tester -d noteful -f ./seeds/seed.noteful.sql